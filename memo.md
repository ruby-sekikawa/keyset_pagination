## CREATE EXTENSION IF NOT EXISTS

共通の文法：CREATE EXTENSION IF NOT EXISTS ...

PostgreSQL は「本体（コア）」に加えて、追加機能をプラグインのように後付けできる仕組みを持っていて、それが extension です。標準で同梱されているものも、CREATE EXTENSION で明示的に「このデータベースで使う」と宣言して初めて有効になります。

- IF NOT EXISTS = 「すでに有効化済みなら何もしない（エラーにしない）」。何度実行しても安全（冪等）にするための書き方です。セットアップスクリプトを再実行してもコケません。

---
① pg_stat_statements — クエリの実行統計を記録する

有効にすると、DB が実行された全SQLの統計（実行回数・合計時間・平均時間・読んだブロック数など）を自動で溜めてくれます。

-- 例：遅いクエリTOP5を探す
SELECT query, calls, mean_exec_time
FROM pg_stat_statements
ORDER BY mean_exec_time DESC
LIMIT 5;

この計画書では Phase 7（発展課題 §9.1） で「どのフィルタの組み合わせが実際に使われているか、pg_stat_statements で実測する」という場面で使います。「勘でインデックスを張るな、実測しろ」という規律のための道具です。

▎ 補足：これは shared_preload_libraries にロードが必要なので、docker-compose.yml の起動オプションに shared_preload_libraries=pg_stat_statements を入れてあります。だから CREATE EXTENSION が成功しました。

---
② pg_prewarm — キャッシュを意図的に温める

PostgreSQL はよく使うデータページをメモリ（バッファキャッシュ）に載せて高速化します。起動直後やデータ投入直後は、このキャッシュが空（=コールドキャッシュ）で、初回アクセスはディスクから読むため遅くなります。

pg_prewarm は、特定のテーブルやインデックスを先読みしてキャッシュに強制的に載せる関数を提供します。

-- orders テーブルをまるごとキャッシュに載せる
SELECT pg_prewarm('orders');

なぜ計測実験で重要かというと、この計画書の随所（§4.3, §11）に出てくる論点です：

▎ 「2回目の実行が急に速い」＝バッファキャッシュが効いている

計測するとき、キャッシュの状態が揃っていないと比較が公平になりません。「1回目は遅くて2回目は速い」のは、クエリの良し悪しではなく単にキャッシュの差だからです。pg_prewarm で毎回同じ「温まった状態」を作れば、条件を揃えた公平な比較ができます。

---


どちらも「計測してから最適化する」という、この課題の核心（§0）を支える観測・実験の道具です。両方すでに有効化済みなので、必要になった Phase でそのまま使えます。

## shared_buffers

### ① shared_buffers = 1GB — DBが握るキャッシュ用メモリ

shared_buffers は、PostgreSQL がテーブルやインデックスのデータページをメモリ上にキャッシュしておく領域のサイズです。ディスクから読んだページをここに載せておき、次に同じページが必要になったらメモリから返す → 速い。

なぜ既定の128MBだと観察しづらいのか：

この課題のテーブルは1000万行で約600MB＋インデックス。既定の128MBだとキャッシュに一部しか乗りません。すると：

- 実行するたびに「キャッシュに乗ってる/乗ってない」がコロコロ変わり、時間が安定しない
- 「今のはディスクを読んだ遅さ？キャッシュヒットの速さ？」が切り分けにくい

1GB に広げてテーブルがほぼ丸ごと乗るようにすると、キャッシュの効き方を意図的にコントロールして観察できます。計画書§4.3 の「2回目の実行で read が hit に変わる（＝キャッシュ効果）」を再現するための土台です。

---
### ② work_mem = 16MB — 1回のソート/ハッシュに使えるメモリ

work_mem は、ORDER BY のソートや JOIN のハッシュ処理などをメモリ内で完結できる上限です。この上限を超えると、PostgreSQL は処理をディスク上の一時ファイルに退避します（＝「ディスクに落ちる」）。メモリ内ソートに比べて桁違いに遅くなります。

「小さいとSortが即ディスクに落ちる」の意味：

インデックスが無い状態（Phase 2）で ORDER BY created_at DESC をやると、大量の行をソートします。work_mem が小さすぎると、そのソートがすぐ一時ファイル行き（external merge Disk と EXPLAIN に出る）になります。

16MB に上げておくと「メモリ内ソート（quicksort Memory / top-N heapsort）」で収まりやすくなり、「インデックスが無いからソートが発生している」という本質を、「work_mem が足りなくてディスクに落ちた」という別要因と混同せずに観察できます。

▎ 補足：shared_buffers は DB全体で共有する1つの大きな領域ですが、work_mem はクエリの処理ステップごと・接続ごとに個別に確保される点が違います。だから大きくしすぎると同時接続数×ステップ数だけメモリを食う、という注意があります（学習環境なので16MBで十分）。

---
### ③ track_io_timing = on — EXPLAIN に「I/Oにかかった時間」を出す

これを on にすると、EXPLAIN (ANALYZE, BUFFERS) の出力に ディスクI/Oに実際に何ミリ秒使ったか（I/O Timings: read=... write=...）が表示されるようになります。既定は off で、この行は出ません。

なぜ効くのか（計画書§1.2の「地味だが効く」）：

クエリが遅いとき、原因は大きく2つに分かれます：

- CPUが遅い：ソートや行の突き合わせなど計算に時間を使っている
- I/Oが遅い：ディスクからページを読むのに時間を使っている

track_io_timing = on があると、EXPLAIN を見た瞬間に「この1.2秒のうち0.9秒はディスク読み込みだった」のように内訳が数字で分かります。これがこの課題の核心テーマ「時間ではなくブロック数・I/O で考える」（§4.3, §0）を支える計器です。

## CPUが遅いとI/Oが遅いの違い

データベースのクエリが「遅い」とき、原因はほぼこの2つのどちらか（または両方）です。作業の性質が根本的に違うので分けて考えます。

---
**前提：クエリは「読む」と「処理する」の2工程でできている**

クエリ実行を料理にたとえると：

1. 材料を冷蔵庫（ディスク）から取り出して台所（メモリ）に並べる → これが I/O
2. その材料を切る・並べ替える・選り分ける → これが CPU

「料理が遅い」とき、冷蔵庫の奥から材料を探すのに時間がかかっているのか、切ったり並べたりに時間がかかっているのか。原因が違えば対処も違う、という話です。

---
### I/O が遅い（I/O bound）

ディスクからデータページを読み込むのに時間を使っている状態です。

- PostgreSQL はデータを「ページ（1ページ = 8KB）」単位でディスクからメモリに読みます
- メモリ（shared_buffers）に無ければディスクへ取りに行く。ディスクはメモリより桁違いに遅い（メモリ: ナノ秒、SSD: マイクロ秒、HDD: ミリ秒）
- 大量のページを読む必要がある = I/O が遅くなる

この課題で起きる典型例：

Phase 2（インデックス無し）で WHERE status = 1 を実行すると、テーブル全体（600MB / 数万ページ）をディスクから読んで、そこから20行を選びます。ほとんどの時間が「読み込み」に消えます。これが I/O bound。

EXPLAIN での見え方：
Buffers: shared read=80000        ← 8万ページも読んでいる
I/O Timings: read=900.123 ms      ← そのうち0.9秒がディスク読み込み

対処：読むページ数を減らす。→ インデックスを張って「必要なページだけ」読むようにする。これがこの課題の主題です。

---
### CPU が遅い（CPU bound）
- 大量のページを読む必要がある = I/O が遅くなる

この課題で起きる典型例：

Phase 2（インデックス無し）で WHERE status = 1 を実行すると、テーブル全体（600MB / 数万ページ）をディスクから読んで、そこから20行を選びます。ほとんどの時間が「読み込み」に消えます。これが I/O bound。

EXPLAIN での見え方：
Buffers: shared read=80000        ← 8万ページも読んでいる
I/O Timings: read=900.123 ms      ← そのうち0.9秒がディスク読み込み

対処：読むページ数を減らす。→ インデックスを張って「必要なページだけ」読むようにする。これがこの課題の主題です。

---
CPU が遅い（CPU bound）

読み込んだデータをメモリ上で処理するのに時間を使っている状態です。ディスクは関係なく、計算そのものが重い。

代表的な重い処理：
- ソート（ORDER BY … 大量の行を並べ替える）
- フィルタ（Rows Removed by Filter … 読んだ行を1つずつ条件チェックして捨てる）
- 集計・JOIN（大量の行の突き合わせ）

この課題で起きる典型例：

ORDER BY created_at DESC で、インデックスが無いと読み込んだ数百万行をメモリ上で全部並べ替えます。データがキャッシュに乗っていて（I/Oは速い）も、この並べ替え計算に時間がかかる。これが CPU bound。

EXPLAIN での見え方：
Sort  (actual time=850..900 ms)
Sort Method: quicksort  Memory: 15000kB
Buffers: shared hit=80000         ← ページは全部キャッシュから(hit)＝ディスクは速い
I/O Timings: read=2 ms            ← I/Oはほぼゼロ。なのに遅い＝CPUが原因

対処：計算量を減らす。→ 適切なインデックスでソート自体を消す（計画書§5.2 の「Sort ノードが消える」）。並べ替え済みのデータを順に読むだけにすれば、CPUの重い仕事が無くなります。

✅ Phase 2 完了：ベースライン計測（インデックスなし）

共通プラン：Limit → Gather Merge(2並列) → Sort → Parallel Seq Scan

┌───────────┬─────────────────────┬─────────────────────────────┬─────────────────────────────────┐
│  OFFSET   │ 実行時間(cold/warm) │           Buffers           │            Sort方式             │
├───────────┼─────────────────────┼─────────────────────────────┼─────────────────────────────────┤
│ 0         │ 2604 / 314ms        │ hit=73,602                  │ top-N heapsort 27kB（メモリ内） │
├───────────┼─────────────────────┼─────────────────────────────┼─────────────────────────────────┤
│ 100,000   │ 1126 / 583ms        │ hit=73,602 + temp書込30,863 │ external merge Disk ~88MB       │
├───────────┼─────────────────────┼─────────────────────────────┼─────────────────────────────────┤
│ 1,000,000 │ 807 / 662ms         │ hit=73,602 + temp書込30,863 │ external merge Disk ~94MB       │
└───────────┴─────────────────────┴─────────────────────────────┴─────────────────────────────────┘

予測 vs 実測

┌───────────────┬─────────────────────────┬────────────────────────────────┬──────────────────────────┐
│               │          予測           │              実測              │           判定           │
├───────────────┼─────────────────────────┼────────────────────────────────┼──────────────────────────┤
│ Q1 実行時間   │ 800ms                   │ cold 2604 / warm 314ms         │ キャッシュで桁が動く     │
├───────────────┼─────────────────────────┼────────────────────────────────┼──────────────────────────┤
│ Q2 OFFSET倍率 │ 1.2倍                   │ 1.85倍(0→100k)、線形にはならず │ 方向は当たり・真因を外す │
├───────────────┼─────────────────────────┼────────────────────────────────┼──────────────────────────┤
│ Q3 読む行数   │ 1000万行/73,000ブロック │ 全1000万走査・hit 73,530       │ 的中                     │
└───────────────┴─────────────────────────┴────────────────────────────────┴──────────────────────────┘

このフェーズの核心（3つの学び）

1. work_mem を超えるとソートが断崖でディスクに落ちる — OFFSET 0 は20行だけ保持で top-N heapsort（メモリ27kB）。OFFSET が増えると OFFSET+LIMIT 行を保持する必要が出て work_mem 16MB を超え、external merge Disk（80〜94MB退避）に切替。これが遅さの正体。§1.2 で work_mem=16MB にした狙いがここで回収されました。
2. OFFSET のコストは「テーブル読込」でなく「ソート量」に出る — Buffers: shared hit=73,602 は全 OFFSET で不変。つまり「20行返すのに毎回全表7.3万ブロックを読み、さらに大量行をソート＆退避して大半を捨てている」。ブロック数で見ると無駄が一目瞭然。
3. 1回の数字を信じない — 同じクエリでも temp write I/O が 1093ms→322ms と変動。§11「比較は必ず複数回」の実証。

チェックポイント（§4）

┌───────────────────────────────────────┬──────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                 項目                  │                                                       結果                                                       │
├───────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 予測と実測のズレを記録                │ ✅                                                                                                               │
├───────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ OFFSET が増えるとコスト増を数字で確認 │ ✅（ただし線形ではなく work_mem 溢れの断崖として現れた。純粋な線形増加は index がある Phase 6 で観察予定と補足） │
├───────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Sort/top-N heapsort ノードの存在      │ ✅ 両方観測（memory版とdisk版）                                                                                  │
└───────────────────────────────────────┴──────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

補足：チェックポイントの「線形に増える」は、インデックスが無いと全走査コストに埋もれて綺麗な線形にはなりません。ここは正直に記録しました。純粋なOFFSETの線形コストは Phase 6 でインデックス下で観察できます。